# Sister-session brief: #640 contracts spec + daemon domain integration

## Your assignment

GitHub issue: https://github.com/drewdrewthis/git-orchard-rs/issues/640

Title: **contracts node + discovery: adopt open/closed model (blocked status removed upstream)**

You are the owner of this issue from kickoff to merge. You drive it autonomously.

## Why YOU are here (and what's blocked behind you)

The parent session merged 16 PRs from the `chore/repo-constitution` queue into a single integration branch (`chore/repo-constitution`, currently at `/Users/USER/workspace/git-orchard-rs-worktrees/constitution`, 38 commits ahead of origin, **NOT pushed**).

Local validation found **9 test failures** in `daemon/features_test/` after the merge. The runtime (`cargo build`, `go build`, `make check-feature-parity`) passes. Only the test infrastructure has gaps. The parent decided these failures are not shippable as-is and that #640 must be delivered first because the contracts domain ownership is the highest-risk unresolved domain split in the integration.

You are NOT just doing #640's spec work. The parent's acceptance criteria for the eventual integration PR is:

> "AC here includes a functional, domain driven daemon that is successfully consumed by the tui and the gui, and via the cli as orchard daemon query."

So your scope is:

1. **Primary (this issue, #640):** make the contracts plugin the spec owner — `spec/` directory, owner-inherit on update, daemon observes via `status != "delivered"`, marketplace install path, ADR.
2. **Required for AC:** the daemon must be **functional + domain-driven + consumed by TUI/GUI/CLI**. That means the integration branch's 9 test failures get fixed (test infra holes in `daemon/features_test/helpers_test.go` for `claudeaccount` / `repos` / `tmux` providers; one schema regression where `Worktree.repo` became a `String` scalar instead of object-with-`id`).
3. **Required for AC:** verify the CLI surface — `orchard daemon query` must work end-to-end. If it doesn't exist yet, file the missing piece.

## What the parent already did (do NOT redo)

The parent session is `/Users/USER/workspace/git-orchard-rs-worktrees/constitution` at branch `chore/repo-constitution` (HEAD `a8c6033`). Read the recent commits to understand:

```bash
cd /Users/USER/workspace/git-orchard-rs-worktrees/constitution
git log --oneline origin/main..HEAD
```

Notable parent commits:
- `55f756f` — events.rs:289 atomicity fix (DO NOT amend; held 5x federation)
- `ffcda6b` — `daemon/git/scripts_test.go` path rewrites (`git-<op>.sh` → `git/<op>.sh`)
- `a8c6033` — `helpers_test.go` partial fix (added `hp.Start(ctx)` for host provider only; other providers still need same treatment)

DO NOT push that branch. DO NOT amend its commits.

## Constraints (hard)

- **Never work on main.** Always in your own worktree.
- **Never push --force to anything.**
- **Never push the `chore/repo-constitution` branch** until parent says so.
- Pre-commit hook blocks personal names (Drew/Andrew/Joia/Garde) except allowlisted: `drewdrewthis` handle, `andrew@langwatch.ai` email, `@andrew` mention, `/ask-drew` skill, `drew-sim` agent, `garde::` Rust crate. Do NOT bypass with `--no-verify`.
- Codex repo (`~/.claude/`) is public — no secrets, no `/Users/<name>/` absolute paths in committed files (you can use them in scratch).
- `/decide` gate for irreversible: merging, pushing to main, deleting branches, force-push, key rotation. Reversible local work: act, log, report.
- No `#[allow(dead_code)]`. Implement or remove.
- Use `/create-skill` for new skills. Never hand-write SKILL.md.
- Never `@unimplemented`. Never `LEGACY_UNBOUND`. Hard parity per T8.

## The path I see

1. **Read the issue thoroughly.** Re-read the body. The spec ownership question is real — the plugin should own valid-status enum + event shape + lifecycle + owner-inherit rule.
2. **Read parent's wisdom file:** `decide-wisdom/2026-05-19-feature-parity-no-unimplemented.md` — explains the testing philosophy that constrains your test work.
3. **Survey the codex plugin source:** `~/.claude/plugins/cache/drewdrewthis-orchard-codex/claude-contracts/0.7.0/` (the live plugin). Read its `spec/` if it exists; if not, you create it.
4. **Inspect the daemon's current contracts observation:** `cd /Users/USER/workspace/git-orchard-rs-worktrees/constitution && grep -rn "ownerAgentName\|contracts" daemon/contracts/ internal/`. Find where the daemon today queries by `ownerAgentName` (wrong-grained per #640) and where it folds status. The fold layer needs to accept legacy `blocked` → `open`.
5. **Spec the changes.** Write a plan in this worktree under `decide-wisdom/<today>-<slug>.md` describing the chosen path. /decide it.
6. **Plan the test-failure fix as part of the work.** Don't merge constitution's failures forward; fix in YOUR branch, then the constitution branch rebases onto you (or you rebase onto it — whichever the merge graph wants).
7. **Drive the PR.** When green CI + reviewed + AC met, open PR to main. The integration PR (chore/repo-constitution) comes AFTER this PR merges, because the constitution branch will rebase onto the new contracts work.

## Subagents available

- `coder` — implementation. Hand it failing tests, it makes them pass.
- `test-expert` — write failing tests from AC, OR review existing tests.
- `Plan` — architect agent.
- `principles-reviewer` / `hygiene-reviewer` / `security-reviewer` / `test-reviewer` — review.
- `devils-advocate` — stress-test before committing.

Use them. You orchestrate; you don't write code directly unless trivial.

## Acceptance for this session

You're done when one of:

(a) PR opened, CI green, AC items 1-7 in the issue are demonstrably met, sister session reports back with the PR URL and a delivered contract.

(b) You hit a one-way door (a /decide escalation) and surface to parent with a specific yes/no question.

(c) The work is materially larger than one session — you file sub-issues with clear AC, contract them, and surface back.

## Open contracts to file

File a contract for this work immediately on boot:

```
mcp__plugin_claude-contracts_contracts__add_contract({
  summary: "Deliver #640: contracts plugin owns spec; daemon observes via open/closed model; CLI + TUI + GUI consume cleanly; integration branch tests green",
  reasoning: "Parent session paused chore/repo-constitution integration PR pending #640 delivery. Sister-session kickoff per parent direction 2026-05-19.",
  source: "issue:drewdrewthis/git-orchard-rs#640"
})
```

## First action

Read the issue body in full (don't trust this brief — verify):

```bash
gh issue view 640 --repo drewdrewthis/git-orchard-rs
```

Then read the parent's recent state:

```bash
cd /Users/USER/workspace/git-orchard-rs-worktrees/constitution
git log --oneline -20 origin/main..HEAD
cd /Users/USER/workspace/git-orchard-rs-worktrees/issue640-contracts-spec
```

Then file the contract above and `/decide` your way into the first concrete action.

Go.
