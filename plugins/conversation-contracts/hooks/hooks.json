{
  "description": "Conversation-contracts hooks: auto-open the conversation contract once on SessionStart (sentinel echoed to stdout, harness records it in the jsonl); surface loose-ends inventory at Stop.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/on-session-start.sh\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/on-stop.sh\""
          }
        ]
      }
    ]
  }
}
