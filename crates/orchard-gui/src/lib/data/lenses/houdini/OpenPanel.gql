query OpenPanel($paneIds: [String!], $cwd: String) {
	tmuxPanes(filter: { paneIdIn: $paneIds, cwd: $cwd, command: "claude" }) {
		...PaneCard
	}
	claudeInstances {
		...SessionCard
	}
	conversations {
		sessionUuid
		lastSeenAt
		firstSeenAt
		messageCount
		open
		recap
		cwd
		jsonlPath
		agentName
		customTitle
	}
	workView {
		repos {
			id
			slug
			worktrees {
				...WorktreeEnrichment
			}
		}
	}
}
